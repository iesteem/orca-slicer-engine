# 五模型切片失败根因分析

> 引擎版本: v02.01.16 | 日期: 2026-06-25 | commit: 0773324

---

## 总览

| # | 模型 Hash | 错误码 | Plate | 现象 | 是否有 Bug | 与桌面版一致 |
|---|-----------|--------|-------|------|------------|-------------|
| 1 | `25810db28a93bb66` | SLICING_ERROR | plate 1 | 几何体无法切片，重试 3 次全失败 | 无（几何体问题），重试机制可优化 | ⚠️ 部分 |
| 2 | `5161ec40e49ee0ff` | SLICING_ERROR | plate 2 | 空层/零厚度（plates 1,3 正常） | **是** — Z-baking 缓存污染 | ❌ 不一致 |
| 3 | `525bd28b477b900c` | PRINT_WARNING | plate 1 | 0.25-6.73mm 处有空层，模型需修复 | 无（模型问题），severity 映射可优化 | ⚠️ 语义 |
| 4 | `9db13d80153a7213` | BUILD_VOLUME_PARTLY_OUTSIDE | plate 4 | 模型超出打印体积（plates 1-3,5 正常） | **是** — plate_width int 截断差异 | ❌ 不一致 |
| 5 | `7121f657892b095a` | EMPTY_PLATE | plate 2 | 空盘无模型（plate 1 正常） | 无 | ✅ 一致 |

---

## 一、`25810db28a93bb66` — SLICING_ERROR（plate 1）

### 1.1 现象

Plate 1 切片时 `print.process()` 抛出 `SlicingError`/`SlicingErrors`，引擎重试 3 次全部失败。

### 1.2 失败链路

```
process_plate(plate_id=0)
  → apply_model()              // Z-baking, 正常
  → run_validation()           // 通过，继续
  → run_slicing()
      → print.process()        // 抛出 SlicingError（非 "empty initial layer"、非 "No layers were detected"）
      → catch SlicingError
          → 消息不含 "empty initial layer" → 不按 warning 处理
          → 消息不含 "No layers were detected" → 不按 no-layers 跳过
          → 推入 SLICING_ERROR + return false
  → retry loop: 清除 errors, 重建 Print → apply_model → run_slicing → 相同异常
  → 重试 3 次耗尽 → set_error_type(EXIT_EXPORT_ERROR)
```

### 1.3 根因分析

**模型几何体本身不可切片。** 可能的几何体缺陷：

| 候选原因 | 说明 |
|----------|------|
| 极薄壁特征 | 壁厚 < 喷嘴直径，Arachne/Classic 无法生成有效的 perimeter |
| 非流形/自相交几何体 | 导致 CGAL 布尔运算（支撑生成）失败 |
| 对象在打印板下方 | Z 范围异常导致层高计算出错 |

`GeometryCheck` 仅在切片前产生 **warning**，不阻断切片。模型即使有非流形边、退化面、自相交等缺陷，仍会进入切片阶段。

### 1.4 重试机制的浪费

每次重试创建全新的 `Print` 对象，但输入数据（模型网格、配置、预设）完全不变。对于几何体级别的 `SlicingError`，重试是**确定性的失败** — 3 次重试浪费 CPU 资源且无意义。

重试机制的设计初衷是处理擦洗塔工具更换不匹配（会修改 `enable_prime_tower` 配置），对几何体切片失败不适用。

### 1.5 次要问题：退出码覆盖

重试耗尽后代码设置 `set_error_type(EXIT_EXPORT_ERROR)`（5），覆盖了真正的 `EXIT_SLICING_ERROR`（4）。虽然 JSON output 中的 issue code 仍是 `"SLICING_ERROR"`，但进程退出码会误导下游消费者。

### 1.6 与桌面版一致性

| 方面 | 云引擎 | 桌面版 |
|------|--------|--------|
| SlicingError 检测 | ✅ 一致 | ✅ 一致 |
| 重试行为 | 对不变输入盲目重试 3 次 | 仅在 UI 驱动状态变化时重试（加载新预设、修改对象等） |
| 退出码 | EXIT_EXPORT_ERROR（误导） | 无对应 CLI 退出码概念 |

**结论：⚠️ 部分一致。** 错误检测一致，但重试策略和退出码不合理。

### 1.7 建议

1. `SlicingError`/`SlicingErrors` 不应触发重试 — 它们是确定性失败，应直接返回或跳过该 plate
2. 在重试耗尽后设置 `EXIT_SLICING_ERROR` 而非 `EXIT_EXPORT_ERROR`
3. 在 `run_slicing()` 的 catch 块中将 `ex.what()` 写入 issue message，便于定位具体原因

---

## 二、`5161ec40e49ee0ff` — SLICING_ERROR 空层/零厚度（plate 2）🔴 P0

### 2.1 现象

Plate 2 切片报 "No layers were detected"（空层/零厚度），但 plates 1 和 3 正常切片。

### 2.2 失败链路

```
process_plate(plate_id=0)   // Plate 1: 正常
  → apply_model() → Z-baking 修改 instance/volume offsets
  → print.apply() → update_slicing_parameters()
      → model_object->max_z() → update_min_max_z()
          → m_min_max_z_valid = false → 首次计算并缓存 → m_min_max_z_valid = true
  → run_slicing() → 成功

process_plate(plate_id=1)   // Plate 2: 失败
  → restore_baked_z_offsets()    // 恢复 offsets 但不清除缓存
  → apply_model() → Z-baking 用不同实例的 offsets
  → print.apply() → update_slicing_parameters()
      → model_object->max_z() → update_min_max_z()
          → m_min_max_z_valid = true → 跳过计算，返回 Plate 1 的缓存值！
  → object_print_z_height 基于错误的 max_z 计算
  → 层平面与模型实际 Z 范围无交集 → m_layers.empty() → "No layers were detected"

process_plate(plate_id=2)   // Plate 3: 正常（巧合）
  → 若 Plate 3 的 instance Z offset 与 Plate 1 相同 → 缓存值碰巧正确 → 切片成功
```

### 2.3 根因：`ModelObject::update_min_max_z()` 缓存未失效

**位置**: `OrcaSlicer/src/libslic3r/Model.cpp:1455-1483`

`m_min_max_z_valid` 标志在首次计算后置为 `true`，之后永远不再重新计算。缓存仅在 `invalidate_bounding_box()` 被调用时清除，而以下代码路径**均未调用**它：

- `restore_baked_z_offsets()` — `SliceEngine.cpp:2030-2042`
- Z-baking 代码 — `SliceEngine.cpp:1982-2017`
- `ModelInstance::set_offset()` — `Model.cpp`
- `ModelVolume::set_offset()` — `Model.cpp`

此外，`update_min_max_z()` 仅使用 `instances.front()` 的变换矩阵，在多实例对象中可能使用了错误的实例。

### 2.4 与桌面版一致性

❌ **不一致。** 桌面版 UI 每次切片都是用户主动触发，中间可能伴随对象移动/修改操作，不存在跨 plate 的缓存污染场景。这是引擎独有的 bug。

### 2.5 修复方案

**修复位置**: `src/SliceEngine.cpp`

```cpp
// 方案 A: 在 restore_baked_z_offsets() 末尾添加
void SliceEngine::restore_baked_z_offsets() {
    for (auto it = m_baked_instance_z.rbegin(); ...) {
        it->inst->set_offset(it->inst_offset);
        for (auto& [vol, offset] : it->volume_offsets) {
            vol->set_offset(offset);
        }
    }
    m_baked_instance_z.clear();

    // 修复: 失效所有 ModelObject 的 bounding box 缓存
    for (ModelObject* obj : m_model.objects) {
        if (obj) obj->invalidate_bounding_box();
    }
}

// 方案 B: 在 apply_model() 的 Z-baking 完成后同样调用
// Z-baking 修改 volume offsets 后:
for (ModelObject* obj : m_model.objects) {
    if (obj) obj->invalidate_bounding_box();
}
```

**推荐**: 方案 A + B 同时实施，双重保障。

---

## 三、`525bd28b477b900c` — PRINT_WARNING 空层 0.25-6.73mm（plate 1）

### 3.1 现象

Plate 1 切片成功但报告 PRINT_WARNING：在 Z 高度 0.25mm 到 6.73mm 之间存在空层。

### 3.2 失败链路

```
run_slicing() → 成功
export_gcode()
  → print.export_gcode()
      → GCode.cpp:1520-1540 collect_layers_to_print()
          → 逐层检查 has_extrusions()
          → 0.25mm 处最后有挤出，6.73mm 处恢复挤出
          → 中间所有层无挤出 → 记录 EmptyGcodeLayers (WarningLevel::CRITICAL)
  → 引擎收集 PrintBase warnings (SliceEngine.cpp:2203-2214)
      → CRITICAL → make_error(plate_id, "PRINT_WARNING", message)
      → issue level = "error", code = "PRINT_WARNING"
  → G-code 正常导出（不阻断）
```

### 3.3 根因分析

模型在 0.25-6.73mm 高度范围内无可打印几何体。可能原因：

| 候选原因 | 说明 |
|----------|------|
| 极薄壁特征 | 壁厚 < 挤出宽度，Arachne/Classic 无法生成 toolpath |
| 中空结构 | 模型在该 Z 范围有内部空腔，shell 不足以容纳 perimeter |
| 分离式几何体 | 模型由两个独立组件组成（0-0.25mm + 6.73mm+），中间无连接 |

`GeometryCheck` 不检测"特征太薄无法挤出"这类问题，只能在 G-code 导出时由 PrintBase 运行时检测。

### 3.4 Severity 语义问题

| 上下文 | 行为 | 阻断导出？ |
|--------|------|-----------|
| 桌面 GUI | `SeriousWarningNotificationLevel` 通知 | 否 |
| 桌面 CLI (`--no_check=false`) | `CLI_SLICING_ERROR`, `flush_and_exit()` | 是 |
| 桌面 CLI (`--no_check=true`) | Warning only | 否 |
| 云引擎 | `make_error(level="error", code="PRINT_WARNING")` | 否 |

代码 `"PRINT_WARNING"` 与 level `"error"` 存在命名矛盾。CRITICAL 级别的 PrintBase 警告被映射为 engine issue level `"error"`，但按照 PrintBase 的设计注释 (`PrintBase.hpp:79`)，这只是 UI 突出显示级别（"用户不能错过"），不代表操作失败。云引擎的语义映射不准确。

### 3.5 与桌面版一致性

⚠️ **功能一致（不阻断），severity 语义不一致。** G-code 正确生成，但 issue 被标记为 `"error"` 级别。

### 3.6 建议

将 CRITICAL PRINT_WARNING 映射为 level `"warning"` + 特殊 code（如 `"PRINT_WARNING_CRITICAL"`），由 API 消费者根据 code 决定显示方式。

---

## 四、`9db13d80153a7213` — BUILD_VOLUME_PARTLY_OUTSIDE（plate 4）🔴 P0

### 4.1 现象

Plate 4（1-based，内部索引 3）模型被判定超出打印体积，但 plates 1-3 和 5 正常。

### 4.2 失败链路

```
process_plate(plate_id=3)   // 内部索引 3 = Plate 4 (1-based)
  → run_build_volume_check()
      → 读取 printable_area: ["0.5x1", "270.5x1", "270.5x271", "0.5x271"]
      → bbox.size().x() = 270.0
      → plate_width  = 270.0 - BED_AXES_TIP_RADIUS(1.25) = 268.75  ← 原引擎值
      → stride = 268.75 * 1.2 = 322.5                               ← 原引擎值
      → 桌面: int(extended_bbox.max.x - extended_bbox.min.x - DefaultTipRadius)
              = int(270.0) = 270                                     ← 扩展框抵消了 DefaultTipRadius
      → 桌面 stride = 270 * 1.2 = 324.0
      → 差值: 324.0 - 322.5 = 1.5mm
      → plate_id=3, col=1, row=1（2列网格）
      → 引擎原点: (322.5, -322.5)
      → 桌面原点: (324.0, -324.0)
      → 引擎 X 偏移比桌面少 1.5mm（模型在引擎局部坐标中左移 1.5mm）
      → 若模型靠近 plate 左边缘（桌面局部坐标 X ≤ 2.0mm）
      → 引擎局部坐标 X < 0.5（printable_area 左边界）
      → 碰撞检测判定 Partly_Outside → BUILD_VOLUME_PARTLY_OUTSIDE error
  → return false（阻断该 plate）
```

### 4.3 根因：两重偏差叠加（`- BED_AXES_TIP_RADIUS` + 缺少 `int` 截断）

**桌面版** `Plater.cpp:15399-15404`:
```cpp
Vec3d max = bed.extended_bounding_box().max;   // 已扩展 min 侧 -DefaultTipRadius
Vec3d min = bed.extended_bounding_box().min;   // min = original_min - DefaultTipRadius
// max.x - min.x - DefaultTipRadius = original_width + DefaultTipRadius - DefaultTipRadius
//                                   = original_width                             ← 抵消！
partplate_list.reset_size(max.x() - min.x() - DefaultTipRadius, ...);  
// reset_size(int, int, ...) 参数为 int → 隐式截断
```

- `original_width = 270.0` (printable_area 的 bbox 宽度)
- 桌面有效值：`int(270.0) = 270`
- 桌面 stride: `270 × 1.2 = 324.0`

**引擎（原代码）** `SliceEngine.cpp:1552-1556`:
```cpp
plate_width = bbox.size().x() - BED_AXES_TIP_RADIUS;  // 270.0 - 1.25 = 268.75, double
```
- 引擎 stride: `268.75 × 1.2 = 322.5`
- **偏差 1**: 多减了 `BED_AXES_TIP_RADIUS`（桌面已通过扩展框抵消）→ 少 1.25mm
- **偏差 2**: 未截断为 `int` → 偏差方向取决于输入值

| Plate (1-based) | col | row | X 偏差 | Y 偏差 | 是否失败 |
|-----------------|-----|-----|--------|--------|---------|
| 1 | 0 | 0 | 0 | 0 | 否 |
| 2 | 1 | 0 | -1.5mm | 0 | 否（除非模型紧贴左边缘） |
| 3 | 0 | 1 | 0 | +1.5mm | 否（Y 上移让模型更靠内） |
| 4 | 1 | 1 | -1.5mm | +1.5mm | **是** |

### 4.4 与桌面版一致性

❌ **不一致。** 引擎多减了 `BED_AXES_TIP_RADIUS`（桌面通过扩展框抵消）且未做 `int` 截断，导致 plate 原点偏差 1.5mm。

### 4.5 修复方案（已应用）

**位置**: `src/SliceEngine.cpp:1571-1572`

```cpp
// 修复: 去除 -BED_AXES_TIP_RADIUS（桌面的扩展框已抵消），添加 int 截断以匹配 reset_size
plate_width  = static_cast<double>(static_cast<int>(bbox.size().x()));
plate_depth  = static_cast<double>(static_cast<int>(bbox.size().y()));
```

同时移除了不再使用的 `constexpr double BED_AXES_TIP_RADIUS = 1.25;`。

---

## 五、`7121f657892b095a` — EMPTY_PLATE（plate 2）✅

### 5.1 现象

Plate 2 被检测为空盘，无模型可打印。Plate 1 正常切片。

### 5.2 检测链路

```
process_plate(plate_id=1)   // 内部索引 1 = Plate 2 (1-based)
  → filter_instances(plate_id=1, identify_ids)
      → 在 m_plate_data 中查找 plate_index == 1 的 PlateData
      → 其 obj_inst_map 为空（3MF XML 中该 plate 无 <object> 子元素）
      → identify_ids 为空
      → 遍历所有 ModelInstance，无 loaded_id 匹配 → instances_on_plate = 0
  → make_warning(plate_id, "EMPTY_PLATE", "Plate 2 has no printable objects...")
  → return（跳过该 plate，不设置 m_any_error）
```

### 5.3 根因分析

3MF 文件中 plate 2 的元数据存在但 `obj_inst_map` 为空。这是正常的桌面版行为 — 用户可能：
- 创建了 plate 2 但从未放置对象
- 将 plate 2 上的对象移动/删除后导出 3MF
- 桌面版保留空 plate 元数据

### 5.4 行为验证

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 索引转换 | ✅ 正确 | XML 1-based → 内部 0-based，一次转换，一致使用 |
| identify_id 映射 | ✅ 正确 | 同一值贯穿 load → storage → matching |
| 错误级别 | ✅ 正确 | `make_warning()`，非 `make_error()` |
| 退出码 | ✅ 正确 | `EXIT_OK`，不设置 `m_any_error` |
| 其他 plate | ✅ 正确 | Plate 1 正常切片 |

### 5.5 与桌面版一致性

✅ **完全一致。** 桌面版同样允许空 plate 存在并正常导出。

---

## 六、修复记录

所有修复已应用到 `src/SliceEngine.cpp`（commit: 0773324 之后）。

### 6.1 P0 修复

| # | Bug | 改动 | 状态 |
|---|-----|------|------|
| 1 | Z-baking 缓存跨盘污染 | `apply_model()` 中 Z-baking 前添加 `invalidate_bounding_box()` 遍历 | ✅ 已应用 |
| 2 | plate_width 偏差（两重：多减 DefaultTipRadius + 缺 int 截断） | 移除 `- BED_AXES_TIP_RADIUS` + `static_cast<int>()` 截断，同步移除 `BED_AXES_TIP_RADIUS` 常量 | ✅ 已应用 |

### 6.2 优化修复

| # | 问题 | 改动 | 状态 |
|---|------|------|------|
| 3 | SlicingError 确定性重试浪费 | 新增 `has_fatal_slicing_error_on_plate()` + 跳过重试 | ✅ 已应用 |
| 4 | 重试耗尽退出码错误 | `EXIT_EXPORT_ERROR` → 保留已有 `EXIT_SLICING_ERROR` | ✅ 已应用 |
| 5 | PRINT_WARNING CRITICAL 映射为 error | `make_error` → `make_warning` + code `PRINT_WARNING_CRITICAL` | ✅ 已应用 |
| 6 | `ex.what()` 写入 issue message | 未实施（需 API 消费者配合，建议后续迭代） | 📋 P3 |

### 6.3 总变更量

```
src/SliceEngine.cpp | +57 −7
1 file changed, 57 insertions(+), 7 deletions(-)
```

### 6.4 对齐桌面版的全局原则

1. **整数截断匹配**: 桌面版使用 `int` 参数类型的地方，引擎必须复现截断行为
2. **缓存失效**: 桌面版每轮切片有完整生命周期，引擎的跨 plate 循环需要额外清除状态
3. **重试语义**: 桌面版的重试由用户交互驱动，引擎应避免对确定性失败盲目重试
4. **错误语义**: issue level 应反映操作结果（成功/失败），而非 UI 突出显示意图
